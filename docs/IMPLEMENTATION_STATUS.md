# 实现与验证状态

更新：2026-09-15。当前为已在 Oracle HTTPS 部署的 **Web Demo**，不是生产级手机产品。

## Hook 与真人钱包验收补充

- 新手机重新配对后，真实手机批准请求 `5a0ad59f-2c45-4130-b343-c65bf977e1b8`；Agent 持续等待、挑战出示并执行，15:45:51 获得演示收件箱签名回执，当时 13 项检查通过。完整流水见 [DEMO_WALKTHROUGH.md](DEMO_WALKTHROUGH.md)。
- 新增窄范围同步 PreToolUse 处理脚本，执行前检查现有手机授权；本机原生审阅为 Trusted。当前 ChatGPT 内置 Codex 活跃会话两次 canary 均执行，热插入未通过，不能宣称当前进程已被拦截。
- 本次 Node24 自动测试共 18 项通过（原有 15 项加 3 项 Hook 测试）；处理函数对旧真人授权返回 grant_time 拒绝。脚本测试不代表原生工具链接入，批准后原生放行流程待下一轮验收。详见 [CODEX_HOOK.md](CODEX_HOOK.md)。

## 本机与 Oracle 验收补充

- 已在用户 PC 安装 Node24、MYA CLI 与 Skill；通过真实手机完成配对、要求修改、修订审批和演示收件箱执行。Agent 应持续等待手机决定，不要求用户在主会话重复确认。
- Oracle 入口：https://mya-esand.duckdns.org/mobile/ 。2026-09-15 已由 9471309 升级至 **0240f65**，发布目录 `/opt/mya-demo/releases/0240f65-20260915`。停服一致性备份的 SQLite 完整性检查通过；见证/资源公钥及私钥文件均未变化，原用户关系 active，原有 2 条执行记录保留。
- 本次新增：手机凭证逐项验签和在线状态、钱包库存/检查、服务器签名挑战、凭证出示和持钥证明、服务签名校验记录、分步验证后执行。没有更换私钥或破坏性数据库迁移。
- 本机 Node24.21.0：构建通过，15 项协议/集成测试通过；真实 Chrome 手机视口及 CLI 测试通过，涵盖新钱包与原有审批/规则全流程。详见 CREDENTIAL_WALLET.md。
- Oracle Node24.21.0：独立安装、构建及 15 项测试均通过；随后从 PC 通过公网 HTTPS 运行同一真实浏览器和 CLI 验收，配对、修改、批准、逐项校验、challenge/present/execute、回执、规则与例外均通过。测试的两组隔离关系在结束时已撤销。
- 对既有用户修订版记录作只读校验：关系/见证/请求/授权/回执签名与对应关系通过，授权当前过期但历史执行处于有效期内。既有记录没有按挑战出示历史；新流程从本版开始留存。
- 以下“首版验证记录”为原始开发环境记录；其中未部署/未使用真实 Codex 的旧结论已被本补充更新。真机 Safari、后台运行、硬件保护仍未完成系统验收。

## 已实现

- 空仓库初始化、完整产品规格和开发/部署/交接文档。
- Node24 + 内置 SQLite 服务；随机生成并持久化见证/资源密钥。
- PC CLI 安装实例、邀请二维码、本地用户确认页面、手机扫码/配对码入口。
- 双端短码、用户关系签名、Agent 持钥证明、独立见证签名。
- 浏览器非导出 CryptoKey + IndexedDB；审批记录应用层加密；Web Locks 串行操作。
- JWS/JWE 的真实签名、加密、认证消息轮询与本地历史。
- 手机规则检查/明确模拟的辅助建议、要求修改、拒绝、模拟指纹确认后真实签名。
- 冻结报告、精确授权对象校验、委托范围、额度、取消、暂停和撤销检查。
- SQLite 原子投递与幂等、资源签名回执；不发送真实邮件。
- 本地 Review、复盘标记、单独启用有限规则、自动批准、站内状态与提醒。
- Codex Skill 及安装器、CLI 示例、服务部署模板。

## 首版验证记录

环境：Linux x86_64，Node v24.19.0；Playwright 1.62.1，Chromium 153.0.8010.0，390×844 手机视口。

| 验证 | 结果 |
|---|---|
| `npm run build` | 通过，手机静态目录构建 |
| `npm test` | 9 个测试通过，0 失败 |
| `npm run test:browser`（指定现有 Chromium） | 通过，真实浏览器交互 |
| Skill quick_validate | 通过 |
| 真实 CLI 子进程 | init、pair、本地确认页面、request、status、execute、doctor 通过 |
| 手机视口布局 | 无横向溢出；审批、规则截图已检查 |

协议/集成测试覆盖：重复 JSON 键、签名篡改、真实 JWE、配对、修改再申请、人工审批与回执、无授权请求、修改内容或收件人、并发重放单次副作用、规则启用/自动/例外/暂停、执行额度、取消撤销、未绑定密钥、模拟凭证默认拒绝、服务重启保持状态、过期授权与过期消息。

浏览器测试覆盖：手机页面加入配对→核对短码→人工确认→要求修改→新版本批准→真实投递回执→规则启用→页面刷新恢复→第二次自动→换接收方转人工→暂停→历史查询；额外使用真实 CLI 进程及 PC 本地确认页完成一次闭环。

浏览器官方下载地址在本执行环境超时，测试使用本地可运行的 Chromium。该运行环境依赖未加入项目运行时；正常开发者可按 README 安装 Playwright Chromium。截图环境补充了中文字体，应用本身使用设备系统字体。

测试发现并修复了一个毫秒级有效期问题：分别读取创建/截止时间，偶尔会超过严格最大期限 1 毫秒；已统一从同一时间戳生成有效窗口。

## 尚未验证或尚未完成

- **真实手机相机/权限、Safari、手机锁屏/省电恢复**：已有用户真机配对和审批反馈，但这些兼容性与恢复情形尚未系统测试，不以手机视口模拟替代真机结论。
- **真实 Codex Skill 与 Oracle HTTPS**：已完成，见上方补充；不能以部署成功代替生产认证。
- **完整 PLAN 验收矩阵**：并非所有 A/W 条目均已执行，尤其真机、组织策略与后续 P1。
- **生产安全及平台认证**：未完成，不提供 runtime attestation 或真实指纹保证。
- **正式 JSON Schema/OpenAPI、保留期限自动清理、任意多租户治理**：未实现，见 DECISIONS/HANDOFF。
- **DID/标准 VC 互通、真实模型、原生 Android/WebAuthn**：明确为后续能力。

自动规则和历史保存在当前浏览器。用户清除站点数据即失去身份和历史，需重新配对；没有恢复功能。服务能更新网页，不能据此宣称运行时完全不受第三方影响。
